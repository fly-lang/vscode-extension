import * as vscode from 'vscode';

export function activate(_context: vscode.ExtensionContext): void {
    // Grammar-based highlighting is registered declaratively via package.json.
    // This entry point is kept for future language-server integration.
}

export function deactivate(): void {}
